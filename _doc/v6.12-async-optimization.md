# v6.12 - 非同步優化

## 日期
2026-05-15

## 主要功能

### 1. WAL Batch Write（批次寫入）
- `Wal.write_batch()` - 批次寫入多頁面
- 減少 I/O 呼叫次數

### 2. Checkpoint 優化
- `DirStorage.needs_checkpoint()` - 檢查是否需要 checkpoint
- `DirStorage.maybe_checkpoint()` - 自動 checkpoint
- 閾值：1000 frames

## 實現狀態

### Phase 1: WAL Batch Write
- [x] Wal.write_batch() 方法
- [x] DirStorage.write_batch() 方法

### Phase 2: Checkpoint
- [x] needs_checkpoint() 方法
- [x] maybe_checkpoint() 方法（自動觸發）